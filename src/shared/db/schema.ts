/**
 * Drizzle schema — skeleton only.
 *
 * Real tables (`producto`, `users`, `sessions`, `catalogs`, `template_config`,
 * `sync_runs` — see design.md → "Database Schema Outline") are added in later
 * PRs, one module at a time, alongside the code that uses them. This file
 * exists so the Drizzle + Drizzle Kit toolchain (`db:generate` / `db:migrate`)
 * is fully wired in Phase 0, without pre-designing tables business logic
 * doesn't exist for yet.
 */

export {};
